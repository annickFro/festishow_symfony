{% extends 'base.html.twig' %}

{% block title %}Style index{% endblock %}

{% block body %}
    <section class="index style container">

        <h2>Add a new style</h2>

        <div class="style-form">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" class="submit" value="{{ button_label|default('Save') }}">
            {{ form_end(form) }}
        </div>

        {% if errors.style_name is defined %}
            <p class="error"><span> {{ errors.style_name }} </span></p>
        {% endif %}
        {% if errors.insert is defined %}
            <p class="error"><span> {{ errors.insert }} </span></p>
        {% endif %}

        <h2>The different styles</h2>

        <ul>
            {% for style in styles %}
                <li>
                    {% include 'component/_cardStyle.html.twig' %}
                </li>
            {% else %}
                <li>There is no style for now 🤷🏿‍♂️</li>
            {% endfor %}
        </ul>
    </section>

    {# <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for style in styles %}
            <tr>
                <td>{{ style.id }}</td>
                <td>{{ style.name }}</td>
                <td>
                    <a href="{{ path('app_style_show', {'id': style.id}) }}">show</a>
                    <a href="{{ path('app_style_edit', {'id': style.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}
{% endblock %}
