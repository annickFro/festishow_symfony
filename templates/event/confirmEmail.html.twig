<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Reservation</title>
</head>
<body>
    <h1>Event {{ event.name }}</h1>
    <p>Hello {{ app.user.username }},</p>
    <p>We confirm that you have reserved {{ numberOfPlaces }} place(s) for :</p>
    <ul>
        <li>Event : {{ event.name }}</li>
        <li>Date : {{ event.datetime | date("d/m/Y H:i") }}</li>
        <li>Place :{{ event.place }}</li>
        <li>City :{{ event.city }}</li>
    </ul>
    <p></p>
    <p>Best regards,</p>
    <p>Festishow</p>
</body>
</html>